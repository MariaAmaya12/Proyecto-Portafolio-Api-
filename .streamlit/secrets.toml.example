# Example Streamlit secrets for local development or deployment.
# Copy this file to `.streamlit/secrets.toml` and replace the values.
# Do not commit real credentials.

APP_USERNAME = "change-me"
APP_PASSWORD = "change-me"
